{% extends 'base.html.twig' %}

{% block title %}Panel Admin{% endblock %}

{% block body %}
<h2 class="subtitle">Bienvenue , vous etes connecter avec le mail : {{app.user.email}}</h2>


<table class="styled-table center">
    <thead>
        <tr>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        
        <tr>
            <td> <a href="{{path('new_session')}}">ajouter une session</a></td>
        </tr>
        <tr>
            <td><a href="{{path('new_stagiaire')}}">ajouter un stagiaire</a></td>
        </tr>
        <tr>
            <td> <a href="{{path('new_materiel')}}">Ajouter un Type de materiel</a></td>
        </tr>
        <tr>
            <td> <a href="{{path('new_formation')}}">Ajouter une formation</a></td>
        </tr>
        <tr>
            <td> <a href="{{path('new_formateur')}}">Ajouter un formateur</a></td>
        </tr>
       
    <tbody>
</table>

{% endblock %}
